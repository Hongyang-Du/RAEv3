# Stage-1 of the decoupled-fusion plan: train a ViT-XL decoder ON TOP OF the FROZEN
# Stage-0 JEPA k7 fusion (ckpt/stage0-fusion-jepa-depthattn-k7). The combine is loaded
# + frozen via the STAGE0_COMBINE env var (set by the job) -- NOT init_from, since the
# stage-0 ckpt has no decoder. cls_surrogate=false matches the JEPA fusion (grounding
# head carried CLS there). Honest full-feed PSNR expected ~25-26 (much higher => latent
# still too pixel-like = alarm). Same 16ep / disc_start 8 anchor recipe as the other k7.
#
# REQUIRES: STAGE0_COMBINE=/sensei-fs-3/users/hongyangd/ckpt/stage0-fusion-jepa-depthattn-k7/ckpt_latest.pt
# (without it the fusion trains from scratch and collapses -- defeats the point).
combine:
  target: stage1.combine.DepthAttnCombine
  params:
    layers: [11, 13, 15, 17, 19, 21, 23]
    p_drop: 0
    cls_surrogate: false          # matches the JEPA stage-0 fusion
    dim: 1024
    out_dim: 1024
    n_layers: 2
    n_heads: 8
    mlp_mult: 2
decoder:
  config_path: configs/decoder/ViTXL
  latent_dim: 1024
loss:
  lpips_w: 1.0
  sigreg: null
  gan:
    disc_weight: 0.75
    disc_start: 8
data:
  data_dir: /mnt/localssd/imagenet-256
  image_size: 256
  val_npz: data_eval/imagenet-256-val.npz
  val_n: 100
training:
  epochs: 16
  batch_size: 32
  lr: 2.0e-4
  warmup_epochs: 2
  ema_decay: 0.9995
  precision: bf16
  ckpt_every: 1
  log_every: 50
  seed: 42
  out_dir: /sensei-fs-3/users/hongyangd/ckpt/stage1-decoder-jepa-depthattn-k7
probe:
  loo_solo: final
  val_image: assets/samples/sample_1.png
wandb:
  enabled: true
  project: raev3-full
  entity: uscgvl
  name: stage1-decoder-jepa-depthattn-k7
